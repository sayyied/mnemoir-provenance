# Retrieval and context packing

Lexical retrieval preserves exact tokens and filenames. Hybrid retrieval adds deterministic local embeddings and ranking explanation. A semantic score affects ordering but not truth authority.

Context packing trims cited results to an explicit character budget and reports omissions/degradation. The embedding host decides how to render citations and coverage to a model or user.
