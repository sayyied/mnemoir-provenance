# Data model

Canonical tables cover source registry and snapshots, raw events, evidence/provenance edges, actors/scopes, proposals/reviews, memories/versions, retrieval logs, adaptive scores, coordination records, bounded jobs/ticks, audits, benchmarks, experiments, and writeback transactions.

Stable identifiers and hashes make idempotency and provenance inspectable. Timestamps intentionally create new lifecycle records where chronology matters.
