# First source and cited recall

Register or ingest only a caller-supplied controlled source. Raw events are observations, evidence items are retrievable support, and neither is automatically an accepted memory.

Recall first filters source health, authority, privacy, and scope. It then ranks eligible evidence and returns citations plus searched/degraded coverage. Verify both a matching query and an intentionally empty query before integrating the result into a host prompt.
