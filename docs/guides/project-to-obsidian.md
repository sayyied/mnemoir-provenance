# Project to Markdown or Obsidian

Generate the projection from canonical SQLite records into a separate output root. Pages carry non-canonical/no-writeback warnings and stable IDs back to source records. Editing the projection does not mutate memory.

Exclude the generated projection from source ingestion unless an explicit migration use case is reviewed, preventing circular evidence.
