# Ingest controlled sources

Supported controlled patterns include repository documents, caller-supplied completed-turn fixtures, session export JSON/JSONL, Obsidian vault fixtures, and legacy export fixtures. Inputs must be inside an allowed temporary/configured root and remain separate from live private stores.

Derived projections are skipped by default to prevent feedback loops.
