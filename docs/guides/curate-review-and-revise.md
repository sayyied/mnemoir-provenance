# Curate, review, and revise

Create a proposal with evidence IDs or source event IDs. Inspect and edit it. Approve or reject with an attributable reviewer and reason. Only approved proposals can be written. Read back the active version.

To correct a memory, create a revision proposal against the existing memory. Use tombstone for inactivity and rollback for an earlier version; do not delete history silently.
