# Run benchmarks

The built-in harness uses synthetic cases for recall, degradation, curation, adaptive scoring, coordination replay, autonomy receipts, privacy, and portability. Optional external benchmarks require their extras and separately licensed datasets.

Report dataset provenance, metric direction, negative results, and dependency/provider boundaries. Benchmark scores do not establish truth authority.
