# Write a source adapter

Declare source identity/type, authority, health, privacy, scope, stable external references, and idempotency behavior. Accept explicit controlled input; reject traversal, symlink escape, backup trees, unsupported files, and implicit live stores.

Return safe counts/hashes/pointers. Keep ingestion separate from proposal creation and durable promotion. Add success, malformed, missing, unauthorized, and degraded tests.
