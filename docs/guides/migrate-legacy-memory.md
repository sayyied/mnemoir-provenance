# Migrate legacy memory

Inventory source families and licenses, export into caller-controlled fixtures, run dry-readiness checks, ingest idempotently, reconcile counts/hashes, and review proposals before promotion. Preserve source priority and conflict policy.

Do not grant live API access to a legacy backend merely to simplify migration. Keep rollback and source-disposition records private.
