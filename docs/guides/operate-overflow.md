# Operate overflow safely

1. Measure a controlled fixture at below-warning, warning, trigger, limit, and over-limit states. 2. Generate a deterministic plan and confirm proposal mode makes no mutation. 3. Preserve evidence. 4. Issue narrow expiring authorization bound to profile, target, before-hash, operation, policy, and optional proposal. 5. Execute and inspect read-back/receipt.

Simulate stale hash and protected-block target-unreachable paths. Live mode remains disabled until a host adapter and private backup root are configured.
