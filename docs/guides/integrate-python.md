# Integrate with Python

Import `connect` and `initialize_database`, choose an explicit database path, then call controlled ingestion, recall, curation, and maintenance functions with typed scope. Render citations and source coverage in the host.

Use the installed-wheel example in `examples/quickstart/python_quickstart.py`. Treat database ownership, backups, concurrency, privacy, and approval routing as host responsibilities.
