# Health and observability

Health aggregates canonical database reachability, schema, source coverage, retrieval availability, worker/service state, and optional adapter posture. Unknown/uninitialized child state must not be hidden under top-level healthy.

Logs and status expose safe IDs, hashes, counts, and redacted pointers; raw memory and capabilities are excluded.
