# Privacy and data handling

Canonical storage may contain private or sensitive content. Operators choose storage location, retention, backups, source authority, and host rendering. Status surfaces avoid raw payloads. Derived projections are separate and should be treated according to the data they expose.

There is no telemetry by default. Do not submit real memories or databases in support reports.
