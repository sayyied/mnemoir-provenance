# No results or abstention

Check source registration, health, scope grants, privacy, ingestion counts, query terms, and context/result limits. An empty result with complete healthy coverage is normal when no eligible evidence matches.

Do not replace abstention with an uncited model answer unless the host labels it as a separate non-memory response.
