# Installation problems

Confirm Python 3.11/3.12, a fresh virtual environment, wheel compatibility, and writable temp/cache paths. Run `python -m pip check`, `python -c "import mnemoir_provenance"`, and `mnemoir --version`.

Do not add Hermes or benchmark extras until the base import and CLI work.
