# Overflow and trim problems

If pressure is unavailable, repair target discovery/permissions rather than treating it as zero. If the trim target is unreachable, inspect protected blocks and revise the plan; do not force deletion. If proposal output changes, regenerate authorization against the current snapshot hash.
