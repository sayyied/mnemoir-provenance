# Versioning

Package, runtime, CLI, plugin metadata, provenance, changelog, and archives share 0.2.0. The intended stable target is 0.2.0. Pre-release compatibility may change before the stable target.

Database migrations are additive where documented. Back up canonical data and test upgrade/read-back before changing a production installation.
