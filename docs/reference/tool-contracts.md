# Tool and adapter contracts

Host tools must bind one narrow core operation, preserve citations/coverage/abstention, enforce host approval, return leak-safe status, and version their input/output schemas. The generic ports are event, recall, curation, and maintenance.

Tool exposure is evaluated for collisions with host built-ins and retired legacy names before registration.
