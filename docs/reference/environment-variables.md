# Environment variables

`MNEMOIR_DB` selects the SQLite file. `MNEMOIR_ROOT` selects the controlled document root used by source commands. `MNEMOIR_UI_HOST` and `MNEMOIR_UI_PORT` describe local UI preference where the launcher supports them. Optional benchmark/provider variables are read only by their explicit commands.

Environment values never override path, scope, authorization, or privacy validation.
