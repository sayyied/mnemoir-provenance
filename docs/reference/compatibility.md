# Compatibility

Python 3.11 and 3.12: supported candidate targets. Python library and local JSON CLI: supported. Controlled session export: supported format. Hermes: optional reference adapter. Obsidian: controlled import and derived projection. Other named harnesses and MCP clients: integration patterns until separately tested.

No hosted service or remote API compatibility is claimed.
