# Scope and privacy matrix

Profile/session/project/actor/council/source/global scope is evaluated before retrieval and mutation. Privacy classes are public, internal, private, sensitive, plus implementation-defined stricter classes.

A host must map identities explicitly, request only necessary scope, and treat denial as final rather than retrying it as transient.
