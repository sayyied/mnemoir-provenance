# Development setup

Create a Python 3.11 or 3.12 virtual environment, install `.[test]`, run focused tests, then `python -m pytest -q` and `python -m build`. Use only synthetic fixtures. Keep generated candidate output outside the source tree.
