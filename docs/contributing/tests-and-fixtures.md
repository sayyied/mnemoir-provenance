# Tests and fixtures

Fixtures must be deterministic, synthetic, licensed, and free of real profiles, transcripts, paths, credentials, databases, or operational receipts. Test success plus empty/degraded/denied paths. UI claims require real browser interaction, not static HTML assertions alone.
