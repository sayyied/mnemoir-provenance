# Public projection workflow

The repository is generated from a reviewed positive allowlist. Every output maps to source, template, or generator. Public contributions are reviewed normally; maintainers backport generated-core changes to canonical source/templates, regenerate, compare, and preserve authorship.

The public tree has no access to private history. A generated diff never authorizes publication by itself.
